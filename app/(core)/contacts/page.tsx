import "server-only";

import { Suspense } from "react";

export default function ContactsPage() {
  return (
    <div className="container">
      <Suspense>
        <div />
      </Suspense>
    </div>
  );
}
